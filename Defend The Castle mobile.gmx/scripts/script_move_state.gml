///move player according to input

if (object_input_manager.right_key) {
    x += 10;
}

if (object_input_manager.left_key) {
    x -= 10;
}

if (object_input_manager.down_key) {
    y += 10;
}

if (object_input_manager.up_key) {
    y -= 10;
}
